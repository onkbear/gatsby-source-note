{
  "name": "gatsby-source-note",
  "version": "1.0.0",
  "description": "Gatsby source plugin for getting articles from note",
  "main": "index.js",
  "scripts": {
    "build": "babel src --out-dir . --ignore \"**/__tests__\"",
    "watch": "babel -w src --out-dir . --ignore \"**/__tests__\"",
    "prepare": "cross-env NODE_ENV=production npm run build"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "gatsby-source-plugin",
    "note"
  ],
  "author": "onkbear <onkbear@gmail.com>",
  "license": "MIT",
  "peerDependencies": {
    "gatsby": "^2.12.1"
  },
  "dependencies": {
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "babel-preset-gatsby-package": "^0.10.0",
    "cross-env": "^7.0.3"
  }
}
